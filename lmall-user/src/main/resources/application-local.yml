eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/


spring:
  cloud:
    stream:
      bindings:
        test_input:
          destination: TEST
          content-type: application/json
